[gd_resource type="CanvasItemShader" format=1]

[resource]

_code = {
"fragment": "vec3 c1 = vec3(1,0,0.5);
vec3 c2 = vec3(0.26,0.78,1);

vec3 col = vec3(0);
float fade = UV.x;
float perc = abs(sin(TIME/2)) + fade;

col = mix(c1, c2, perc);
COLOR = vec4(col,COLOR.a);",
"fragment_ofs": 0,
"light": "",
"light_ofs": 0,
"vertex": "",
"vertex_ofs": 0
}

